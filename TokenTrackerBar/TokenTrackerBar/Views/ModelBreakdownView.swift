import SwiftUI

/// Deprecated: Merged into TopModelsView.
/// Kept as empty stub to avoid Xcode project file changes.
struct ModelBreakdownView: View {
    let fleetData: [FleetEntry]

    var body: some View {
        EmptyView()
    }
}
