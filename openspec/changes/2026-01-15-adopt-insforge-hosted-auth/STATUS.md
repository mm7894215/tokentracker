# Status: superseded (planning)

This change was scoped to **dashboard hosted auth routes** (InsForge SDK + `/sign-in` / `/sign-up`). It remains valid as **historical context** for frontend routing work, but it is **no longer** the canonical driver for **cloud backend architecture**, ingest contracts, or database design.

**Superseded by:** `openspec/changes/2026-03-28-greenfield-cloud-backend/` (greenfield auth + ingest + retention planning; legacy `insforge-src` / `vibeusage-*` paths explicitly out of scope for new work).

**Action:** Do not extend `tasks.md` here for backend or Edge rewrites; open a new change that references the greenfield spec instead.
