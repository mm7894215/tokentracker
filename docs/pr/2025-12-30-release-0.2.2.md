# PR Template (Minimal)

## PR Goal (one sentence)

Record the 0.2.2 release notes and regression verification after publishing.

## Commit Narrative

- Commit 1: `release: 0.2.2`

## Regression Test Gate

### Most likely regression surface

- Opencode plugin install/uninstall, opencode usage parsing, dashboard install copy.

### Verification method (choose at least one)

- [x] Existing automated tests did not fail (command: `node --test test/rollout-parser.test.js test/init-uninstall.test.js` => PASS)
- [ ] New minimal test added and executed
- [ ] Manual regression path executed

### Uncovered scope

- Live Opencode session end-to-end smoke on a real install.

## Notes

- Release published as `@vibescore/tracker@0.2.2`.
- Git tag: `v0.2.2`.
