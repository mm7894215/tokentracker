# PR Template (Minimal)

## PR Goal (one sentence)

Record the 0.2.5 release notes and regression verification after publishing.

## Commit Narrative

- Commit 1: `release: 0.2.5`
- Commit 2: `docs: record 0.2.5 release verification`

## Regression Test Gate

### Most likely regression surface

- CLI init flow (menu, dry-run preview, Opencode plugin setup, privacy copy).

### Verification method (choose at least one)

- [x] `node --test test/init-uninstall.test.js test/init-spawn-error.test.js test/init-dry-run.test.js` => PASS
- [x] `node scripts/acceptance/npm-install-smoke.cjs` => PASS
- [x] Published package smoke: `VIBESCORE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs` => PASS

### Uncovered scope

- Real-world init on a machine with Codex + Claude + Gemini all installed.

## Notes

- Release published as `@vibescore/tracker@0.2.5`.
- Git tag: `v0.2.5`.
