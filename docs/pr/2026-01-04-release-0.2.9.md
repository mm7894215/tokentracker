# PR Template (Minimal)

## PR Goal (one sentence)

Record the 0.2.9 release notes and regression verification after publishing.

## Commit Narrative

- Commit 1: `release: 0.2.9`
- Commit 2: `docs: record 0.2.9 release verification`

## Regression Test Gate

### Most likely regression surface

- CLI init flow (notify hooks, Opencode plugin install/uninstall).

### Verification method (choose at least one)

- [x] `npm test` => PASS
- [x] `node scripts/acceptance/opencode-plugin-install.cjs` => PASS
- [x] Published package smoke: `VIBESCORE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs` => PASS

### Uncovered scope

- Real-world init on a machine with Codex + Claude + Gemini all installed.

## Notes

- Release published as `vibeusage@0.2.9`.
- Git tag: `v0.2.9`.
