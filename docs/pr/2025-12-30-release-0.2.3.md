# PR Template (Minimal)

## PR Goal (one sentence)

Record the 0.2.3 release notes and regression verification after publishing.

## Commit Narrative

- Commit 1: `release: 0.2.3`
- Commit 2: `docs: record 0.2.3 release verification`

## Regression Test Gate

### Most likely regression surface

- CLI init/uninstall flows (Codex/Every Code/Gemini/Opencode).
- Opencode plugin command template.

### Verification method (choose at least one)

- [x] Existing automated tests did not fail (commands: `node --test test/init-uninstall.test.js`, `node scripts/acceptance/opencode-plugin-install.cjs`, `node scripts/acceptance/gemini-hook-install.cjs` => PASS)
- [x] Published package smoke (command: `VIBESCORE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs` => PASS)

### Uncovered scope

- Real user session end-to-end smoke (Opencode/Gemini) on a live install.

## Notes

- Release published as `@vibescore/tracker@0.2.3`.
- Git tag: `v0.2.3`.
