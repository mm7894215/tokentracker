# PR Template (Minimal)

## PR Goal (one sentence)

Record the 0.2.12 release notes and regression verification after publishing.

## Commit Narrative

- Commit 1: `change(cli): default dashboard url to vibeusage.cc`
- Commit 2: `docs: record 0.2.12 release verification`

## Regression Test Gate

### Most likely regression surface

- CLI init success copy and release packaging smoke.

### Verification method (choose at least one)

- [x] `npm test` => PASS
- [x] `node --test test/rollout-parser.test.js` => PASS
- [x] Pre-publish smoke: `node scripts/acceptance/npm-install-smoke.cjs` => PASS
- [x] Published package smoke: `VIBESCORE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs` => PASS

### Uncovered scope

- Full CLI init run with browser auth in a real environment.

## Notes

- Release published as `vibeusage@0.2.12`.
- Git tag: `v0.2.12`.
